# DO NOT commit real values here — apply manually via kubectl
# Run: kubectl create secret generic shyaway-secrets \
#   --from-literal=DB_HOST=your-host \
#   --from-literal=DB_PORT=22445 \
#   --from-literal=DB_USER=avnadmin \
#   --from-literal=DB_PASSWORD=your-password \
#   --from-literal=DB_NAME=defaultdb

apiVersion: v1
kind: Secret
metadata:
  name: shyaway-secrets
type: Opaque
stringData:
  DB_HOST: "REPLACE_ME"
  DB_PORT: "REPLACE_ME"
  DB_USER: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  DB_NAME: "REPLACE_ME"
